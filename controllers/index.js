module.exports = {
	authController: require('./authController'), //controlador de rutas de autenticacion
	userController: require('./userController'), //controlador de rutas de usuario
	storeController: require('./storeController'), //controlador de rutas de la Tienda
};
